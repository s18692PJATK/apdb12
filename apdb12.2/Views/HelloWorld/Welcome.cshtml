@model object

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>

<ul>
    @for (int i = 0; i < (int) ViewData["NumTimes"]; ++i)
    {
        <li>@ViewData["Message"]</li>
    }
        
</ul>
